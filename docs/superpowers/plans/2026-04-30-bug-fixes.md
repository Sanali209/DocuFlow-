# Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Исправить 18 задокументированных багов во всех системах DocuFlow — от критических ошибок рантайма до некорректной бизнес-логики.

**Architecture:** Исправления сгруппированы по компонентам. Каждый таск независим и покрывается тестами. Не трогаем UI-слой (view.py) — только системный и инфраструктурный код.

**Tech Stack:** Python 3.11+, SQLModel, AnyIO/asyncio, Pydantic, pytest, uv

---

## Файловая карта изменений

| Файл | Багов | Таски |
|---|---|---|
| `src/docuflow/infrastructure/sync.py` | 3 | T1, T7, T8 |
| `src/docuflow/features/chat/system.py` | 1 | T1 |
| `src/docuflow/application/bus/orchestrator.py` | 2 | T2, T3 |
| `src/docuflow/application/bus/dispatcher.py` | 1 | T3 |
| `src/docuflow/features/production/system.py` | 1 | T4 |
| `src/docuflow/features/admin/system.py` | 3 | T4, T5, T6 |
| `src/docuflow/features/folder_scanner/mirror.py` | 1 | T5 (тест) |
| `src/docuflow/features/auth/system.py` | 1 | T6 |
| `src/docuflow/features/inventory/system.py` | 1 | T9 |
| `src/docuflow/features/task_board/system.py` | 1 | T10 |
| `src/docuflow/features/chat/incidents.py` | 2 | T11 |
| `src/docuflow/infrastructure/bus.py` | 1 | T12 |

---

### Task 1: Исправить неверные имена методов (критические)

**Баги:** `sync.py:118` (`broadcast_request` → `broadcast_command`), `chat/system.py:92` (`broadcast_message` → `write_message`), `admin/system.py:4` (unused import)

**Files:**
- Modify: `src/docuflow/infrastructure/sync.py:118`
- Modify: `src/docuflow/features/chat/system.py:92`
- Modify: `src/docuflow/features/admin/system.py:4`

- [ ] **Step 1: Написать failing тесты**

```python
# tests/unit/test_sync_broadcast.py
from unittest.mock import AsyncMock, MagicMock
import pytest
from docuflow.infrastructure.sync import DataSyncSystem

@pytest.fixture
def sync_system(tmp_path):
    from docuflow.infrastructure.config import Config
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    engine = MagicMock()
    system = DataSyncSystem(config, engine)
    return system

@pytest.mark.asyncio
async def test_broadcast_entity_update_calls_broadcast_command(sync_system):
    """broadcast_entity_update должен вызывать broadcast_command, не broadcast_request."""
    orchestrator = AsyncMock()
    orchestrator.broadcast_command = AsyncMock()
    # Убедимся, что broadcast_request НЕ существует
    del orchestrator.broadcast_request
    sync_system.set_orchestrator(orchestrator)

    from sqlmodel import SQLModel, Field
    from typing import Optional
    class FakeEntity(SQLModel):
        id: Optional[int] = Field(default=None)
        updated_at: Optional[object] = None

    entity = FakeEntity()
    await sync_system.broadcast_entity_update(entity)
    orchestrator.broadcast_command.assert_called_once()
```

```python
# tests/unit/test_chat_broadcast.py
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from docuflow.features.chat.system import ChatSystem

@pytest.mark.asyncio
async def test_send_message_calls_write_message_not_broadcast(tmp_path):
    """send_message должен использовать write_message, не broadcast_message."""
    from docuflow.infrastructure.config import Config
    from sqlmodel import Session, create_engine, SQLModel
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)

    config = Config(node_id="TEST", shared_path=str(tmp_path))
    session = Session(engine)

    mock_bus = AsyncMock()
    mock_bus.write_message = AsyncMock(return_value="msg_id")
    # broadcast_message не должен существовать
    assert not hasattr(mock_bus, 'broadcast_message')

    sdk = AsyncMock()
    sdk.resolve_system_by_type = AsyncMock(return_value=mock_bus)

    system = ChatSystem(config, session, sdk=sdk)
    await system.send_message(author="test", content="hello")

    mock_bus.write_message.assert_called_once()
    session.close()
```

- [ ] **Step 2: Запустить — убедиться что падают**

```bash
uv run pytest tests/unit/test_sync_broadcast.py tests/unit/test_chat_broadcast.py -v
```
Ожидаем: FAIL — `AttributeError: ... broadcast_request` / `broadcast_message`

- [ ] **Step 3: Исправить `sync.py:118`**

```python
# src/docuflow/infrastructure/sync.py, строка 118
# БЫЛО:
await self._orchestrator.broadcast_request(CommandType.UPSERT_USER, data)
# СТАЛО:
await self._orchestrator.broadcast_command(CommandType.UPSERT_USER, data)
```

- [ ] **Step 4: Исправить `chat/system.py:89-99`**

```python
# src/docuflow/features/chat/system.py, строка ~89-99
# БЫЛО:
await bus.broadcast_message({
    "action": "CHAT_MESSAGE_NEW",
    "id": new_message.id,
    "author": author,
    "type": message_type.value,
})
# СТАЛО:
await bus.write_message({
    "action": "CHAT_MESSAGE_NEW",
    "id": new_message.id,
    "author": author,
    "type": message_type.value,
})
```

- [ ] **Step 5: Удалить неиспользуемый импорт из `admin/system.py`**

```python
# src/docuflow/features/admin/system.py, строка 4
# УДАЛИТЬ:
from math import log
```

- [ ] **Step 6: Запустить тесты — убедиться что проходят**

```bash
uv run pytest tests/unit/test_sync_broadcast.py tests/unit/test_chat_broadcast.py -v
```
Ожидаем: PASS

- [ ] **Step 7: Коммит**

```bash
git add src/docuflow/infrastructure/sync.py \
        src/docuflow/features/chat/system.py \
        src/docuflow/features/admin/system.py \
        tests/unit/test_sync_broadcast.py \
        tests/unit/test_chat_broadcast.py
git commit -m "fix: correct wrong method names broadcast_request/broadcast_message, remove unused import"
```

---

### Task 2: Исправить обработку ошибок в polling_worker (orchestrator)

**Баг:** `orchestrator.py:185` — сообщения с ошибкой не удаляются из inbox → бесконечный retry loop.

**Files:**
- Modify: `src/docuflow/application/bus/orchestrator.py:175-186`
- Test: `tests/unit/test_orchestrator_polling.py`

- [ ] **Step 1: Написать failing тест**

```python
# tests/unit/test_orchestrator_polling.py
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from docuflow.application.bus.orchestrator import P2POrchestrator

@pytest.fixture
def orchestrator(tmp_path):
    from docuflow.infrastructure.config import Config
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    orch = P2POrchestrator(config)
    return orch

@pytest.mark.asyncio
async def test_failed_message_is_deleted_from_inbox(orchestrator):
    """Сообщение с ошибкой обработки должно удаляться из inbox."""
    # Arrange
    bad_message = {"_filename": "REQ_SENDER_TEST_001.json", "bad": "data"}
    orchestrator._bus = AsyncMock()
    orchestrator._bus.poll_messages = AsyncMock(return_value=[bad_message])
    orchestrator._bus.delete_message = AsyncMock()

    orchestrator._dispatcher = MagicMock()
    # dispatch поднимает исключение (плохая подпись)
    orchestrator._dispatcher.dispatch.side_effect = ValueError("Invalid signature")
    orchestrator._is_running = False  # один цикл

    # Act — вызываем один шаг без бесконечного цикла
    import anyio
    messages = await orchestrator._bus.poll_messages()
    for msg_data in messages:
        filename = msg_data.get("_filename")
        try:
            from docuflow.domain.messages import P2PMessage
            P2PMessage.model_validate(msg_data)
            orchestrator._dispatcher.dispatch(MagicMock())
        except Exception:
            pass
        # ТЕКУЩЕЕ ПОВЕДЕНИЕ: delete_message не вызывается
        # ОЖИДАЕМОЕ: delete_message должен вызываться

    # Assert текущего (сломанного) поведения
    orchestrator._bus.delete_message.assert_not_called()  # ← этот тест пройдёт сейчас (сломано!)
```

> **Примечание:** Тест выше проверяет ТЕКУЩЕЕ сломанное поведение. Когда мы исправим баг, тест нужно перевернуть.

- [ ] **Step 2: Переписать тест для правильного поведения**

```python
# tests/unit/test_orchestrator_polling.py (финальная версия)
from unittest.mock import AsyncMock, MagicMock, call
import pytest
from docuflow.application.bus.orchestrator import P2POrchestrator
from docuflow.infrastructure import constants


@pytest.fixture
def orchestrator(tmp_path):
    from docuflow.infrastructure.config import Config
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    orch = P2POrchestrator(config)
    return orch


@pytest.mark.asyncio
async def test_failed_message_deleted_on_parse_error(orchestrator):
    """Сообщение с ошибкой парсинга должно удаляться из inbox."""
    bad_msg = {"_filename": "REQ_SENDER_TEST_001.json", "invalid": True}
    orchestrator._bus = AsyncMock()
    orchestrator._bus.poll_messages = AsyncMock(return_value=[bad_msg])
    orchestrator._bus.delete_message = AsyncMock()
    orchestrator._dispatcher = MagicMock()
    orchestrator._is_running = True

    # Выполняем один цикл вручную
    messages = await orchestrator._bus.poll_messages()
    for msg_data in messages:
        filename = msg_data.get("_filename")
        try:
            from docuflow.domain.messages import P2PMessage
            p2p_msg = P2PMessage.model_validate(msg_data)
            orchestrator._dispatcher.dispatch(p2p_msg)
            if filename:
                await orchestrator._bus.delete_message(constants.BUS_INBOX_DIR, filename)
        except Exception:
            # После исправления: удалять даже при ошибке
            if filename:
                await orchestrator._bus.delete_message(constants.BUS_INBOX_DIR, filename)

    orchestrator._bus.delete_message.assert_called_once_with(
        constants.BUS_INBOX_DIR, "REQ_SENDER_TEST_001.json"
    )


@pytest.mark.asyncio
async def test_successful_message_deleted_after_dispatch(orchestrator):
    """Успешно обработанное сообщение удаляется из inbox."""
    import time
    from docuflow.domain.messages import CommandType, P2PMessage, P2PPayload
    from docuflow.infrastructure.security import HMACSigner

    signer = HMACSigner("test_secret")
    msg = P2PMessage(
        sender_id="NODE_B",
        sequence=1,
        timestamp=time.time(),
        payload=P2PPayload(command=CommandType.UPSERT_USER, data={}),
    )
    msg.signature = signer.sign(msg.to_signable_content())
    msg_data = msg.model_dump()
    msg_data["_filename"] = "BROADCAST_NODE_B_001.json"

    orchestrator._bus = AsyncMock()
    orchestrator._bus.poll_messages = AsyncMock(return_value=[msg_data])
    orchestrator._bus.delete_message = AsyncMock()

    from docuflow.application.bus.dispatcher import SecureDispatcher
    from docuflow.infrastructure.config import Config
    from docuflow.infrastructure.security import HMACSigner as HS
    config = Config(node_id="TEST")
    dispatcher = SecureDispatcher(config, HS("test_secret"))
    dispatcher.register_handler(CommandType.UPSERT_USER, lambda d: None)
    orchestrator._dispatcher = dispatcher

    messages = await orchestrator._bus.poll_messages()
    for msg_data in messages:
        filename = msg_data.get("_filename")
        try:
            p2p_msg = P2PMessage.model_validate(msg_data)
            orchestrator._dispatcher.dispatch(p2p_msg)
            if filename:
                await orchestrator._bus.delete_message(constants.BUS_INBOX_DIR, filename)
        except Exception:
            if filename:
                await orchestrator._bus.delete_message(constants.BUS_INBOX_DIR, filename)

    orchestrator._bus.delete_message.assert_called_once()
```

- [ ] **Step 3: Запустить — убедиться что первый тест падает**

```bash
uv run pytest tests/unit/test_orchestrator_polling.py::test_failed_message_deleted_on_parse_error -v
```
Ожидаем: FAIL — `delete_message` не вызывается при ошибке

- [ ] **Step 4: Исправить `orchestrator.py` — блок `_polling_worker`**

```python
# src/docuflow/application/bus/orchestrator.py, строки ~175-187
# Заменить внутренний try/except:

for msg_data in messages:
    filename: str | None = msg_data.get("_filename")
    try:
        # 1. Parse raw message
        p2p_msg: P2PMessage = P2PMessage.model_validate(msg_data)

        # 2. Dispatch to domain handlers (includes security check)
        self._dispatcher.dispatch(p2p_msg)
    except Exception as msg_error:
        logger.warning(f"Orchestrator: Failed to process message: {msg_error}")
    finally:
        # 3. Always delete processed/failed message to prevent infinite retry
        if filename:
            await self._bus.delete_message(constants.BUS_INBOX_DIR, filename)
```

- [ ] **Step 5: Запустить — убедиться что оба теста проходят**

```bash
uv run pytest tests/unit/test_orchestrator_polling.py -v
```
Ожидаем: PASS (оба теста)

- [ ] **Step 6: Коммит**

```bash
git add src/docuflow/application/bus/orchestrator.py \
        tests/unit/test_orchestrator_polling.py
git commit -m "fix: always delete inbox message after processing (prevent infinite retry)"
```

---

### Task 3: Исправить sequence race + двойную регистрацию обработчиков

**Баги:**
- `dispatcher.py:92` — sequence обновляется до выполнения handler
- `orchestrator.py:62` — `register_handlers` дважды

**Files:**
- Modify: `src/docuflow/application/bus/dispatcher.py:81-98`
- Modify: `src/docuflow/application/bus/orchestrator.py:61-62`
- Test: `tests/unit/test_dispatcher.py`

- [ ] **Step 1: Написать failing тест для sequence race condition**

```python
# tests/unit/test_dispatcher.py
import time
import pytest
from docuflow.application.bus.dispatcher import SecureDispatcher
from docuflow.domain.messages import CommandType, P2PMessage, P2PPayload
from docuflow.infrastructure.config import Config
from docuflow.infrastructure.security import HMACSigner


def make_message(sender: str, seq: int, secret: str = "test") -> P2PMessage:
    signer = HMACSigner(secret)
    msg = P2PMessage(
        sender_id=sender,
        sequence=seq,
        timestamp=time.time(),
        payload=P2PPayload(command=CommandType.UPSERT_USER, data={}),
    )
    msg.signature = signer.sign(msg.to_signable_content())
    return msg


@pytest.fixture
def dispatcher():
    config = Config(node_id="TEST")
    signer = HMACSigner("test")
    return SecureDispatcher(config, signer)


def test_sequence_not_updated_when_handler_raises(dispatcher):
    """Если handler бросает исключение — sequence не должен обновляться."""
    call_count = 0

    def failing_handler(data):
        nonlocal call_count
        call_count += 1
        raise RuntimeError("handler failed")

    dispatcher.register_handler(CommandType.UPSERT_USER, failing_handler)
    msg = make_message("NODE_B", seq=1)

    with pytest.raises(RuntimeError):
        dispatcher.dispatch(msg)

    # Sequence не должен быть записан
    assert dispatcher._last_sequences.get("NODE_B", -1) == -1, (
        "Sequence не должен обновляться если handler упал"
    )

    # Следующее сообщение с seq=1 должно быть принято (retry)
    msg2 = make_message("NODE_B", seq=1)
    result = dispatcher.dispatch(msg2)  # не должен падать как replay
    assert call_count == 2  # handler вызван снова


def test_sequence_updated_after_successful_handler(dispatcher):
    """После успешного handler — sequence обновляется."""
    dispatcher.register_handler(CommandType.UPSERT_USER, lambda d: "ok")
    msg = make_message("NODE_B", seq=5)
    dispatcher.dispatch(msg)
    assert dispatcher._last_sequences["NODE_B"] == 5


def test_replay_attack_rejected(dispatcher):
    """Повторное сообщение с тем же sequence отклоняется."""
    dispatcher.register_handler(CommandType.UPSERT_USER, lambda d: "ok")
    msg = make_message("NODE_B", seq=1)
    dispatcher.dispatch(msg)

    msg2 = make_message("NODE_B", seq=1)
    with pytest.raises(ValueError, match="Duplicate or out-of-order"):
        dispatcher.dispatch(msg2)
```

- [ ] **Step 2: Запустить — убедиться что первый тест падает**

```bash
uv run pytest tests/unit/test_dispatcher.py::test_sequence_not_updated_when_handler_raises -v
```
Ожидаем: FAIL — sequence обновляется до handler

- [ ] **Step 3: Исправить `dispatcher.py` — переместить обновление sequence**

```python
# src/docuflow/application/bus/dispatcher.py, строки ~81-98
# Заменить блок "Routing" и "Successful processing":

        # 3. Routing
        command: CommandType = message.payload.command
        handler: HandlerFunc | None = self._handlers.get(command)

        if not handler:
            logger.warning(
                f"[{self.config.node_id}] Dispatcher: No handler registered for {command}"
            )
            # Update sequence even for unhandled commands to prevent replay
            self._last_sequences[message.sender_id] = message.sequence
            return None

        logger.info(
            f"[{self.config.node_id}] Dispatcher: Executing {command} "
            f"from {message.sender_id} (seq: {message.sequence})"
        )

        # 4. Execute handler — update sequence ONLY after successful execution
        result = handler(message.payload.data)
        self._last_sequences[message.sender_id] = message.sequence
        return result
```

- [ ] **Step 4: Исправить `orchestrator.py` — убрать дублированный вызов `register_handlers`**

```python
# src/docuflow/application/bus/orchestrator.py

# В __init__ (строки ~61-62): УДАЛИТЬ эти строки:
#   if self._admin_sync and self._dispatcher:
#       self._admin_sync.register_handlers(self._dispatcher)

# В on_startup (строки ~104-106): ОСТАВИТЬ только здесь:
#   if self._admin_sync and self._dispatcher:
#       self._admin_sync.register_handlers(self._dispatcher)
```

- [ ] **Step 5: Запустить все тесты dispatcher**

```bash
uv run pytest tests/unit/test_dispatcher.py -v
```
Ожидаем: все 3 теста PASS

- [ ] **Step 6: Коммит**

```bash
git add src/docuflow/application/bus/dispatcher.py \
        src/docuflow/application/bus/orchestrator.py \
        tests/unit/test_dispatcher.py
git commit -m "fix: update sequence after handler execution, remove duplicate register_handlers call"
```

---

### Task 4: Missing await + deprecated get_event_loop

**Баги:**
- `production/system.py:92` — `broadcast_command` не awaited
- `admin/system.py:53` — `asyncio.get_event_loop()` устарел

**Files:**
- Modify: `src/docuflow/features/production/system.py:88-95`
- Modify: `src/docuflow/features/admin/system.py:53`
- Test: `tests/unit/test_production_broadcast.py`

- [ ] **Step 1: Написать тест для production broadcast**

```python
# tests/unit/test_production_broadcast.py
from unittest.mock import AsyncMock, MagicMock, patch
import pytest


@pytest.fixture
def production_system(tmp_path):
    from docuflow.infrastructure.config import Config
    from docuflow.features.production.system import ProductionSystem
    from sqlmodel import Session, create_engine, SQLModel
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    session = Session(engine)
    return ProductionSystem(config, session)


@pytest.mark.asyncio
async def test_register_finished_pallet_awaits_broadcast(production_system, tmp_path):
    """broadcast_command должен быть awaited при регистрации паллета."""
    # Arrange
    mock_orchestrator = MagicMock()
    mock_orchestrator.broadcast_command = AsyncMock(return_value=None)

    mock_sdk = MagicMock()
    mock_sdk.orchestrator = mock_orchestrator
    production_system.sdk = mock_sdk

    # Создаём минимальный TaskItem в БД
    from docuflow.domain.entities.production import TaskItem, WorkItem, Project
    from sqlmodel import Session, create_engine, SQLModel
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)
    with Session(engine) as session:
        project = Project(name="Test")
        session.add(project)
        session.flush()
        wi = WorkItem(folder_name="test", folder_path=".", project_id=project.id)
        session.add(wi)
        session.flush()
        task = TaskItem(work_item_id=wi.id, file_name="t.gnc", file_path="t.gnc")
        session.add(task)
        session.commit()
        session.refresh(task)
        task_id = task.id

    production_system.session = Session(engine)
    production_system.session.exec  # ensure session works

    # Act
    from docuflow.features.production.system import ProductionSystem
    # Патчим сессию для доступа к task_id
    production_system.db_session.add = MagicMock()
    production_system.db_session.flush = MagicMock()
    production_system.db_session.refresh = MagicMock()
    production_system.db_session.get = MagicMock(return_value=None)

    with patch.object(production_system, 'create_unique_pallet_label', return_value='26-04-TEST-0001'):
        production_system.register_finished_pallet(task_item_id=1, quantity=10)

    # Assert: broadcast_command должен быть вызван и awaited
    # Без await создаётся coroutine object — проверяем что AsyncMock был вызван
    mock_orchestrator.broadcast_command.assert_called_once()
```

- [ ] **Step 2: Запустить — убедиться что тест падает**

```bash
uv run pytest tests/unit/test_production_broadcast.py -v
```
Ожидаем: FAIL или WARNING о coroutine never awaited

- [ ] **Step 3: Исправить `production/system.py:88-95`**

```python
# src/docuflow/features/production/system.py, строки ~88-95
# БЫЛО:
        if self.sdk and hasattr(self.sdk, "orchestrator") and self.sdk.orchestrator:
            self.sdk.orchestrator.broadcast_command(
                command="SYNC_PALLET",
                data={"label_id": unique_label, "qty": quantity, "task_id": task_item_id},
            )

# СТАЛО — broadcast_command это async, нужно создать background task:
        if self.sdk and hasattr(self.sdk, "orchestrator") and self.sdk.orchestrator:
            import asyncio
            asyncio.get_event_loop().create_task(
                self.sdk.orchestrator.broadcast_command(
                    command="SYNC_PALLET",
                    data={"label_id": unique_label, "qty": quantity, "task_id": task_item_id},
                )
            )
```

> **Примечание:** `register_finished_pallet` — синхронный метод. Нельзя `await` внутри него. Оборачиваем в `create_task`.

- [ ] **Step 4: Исправить `admin/system.py:53`**

```python
# src/docuflow/features/admin/system.py, строки ~51-56
# БЫЛО:
    def _create_background_task(self, coro: Any, name: str = "") -> asyncio.Task:
        """Create a tracked background task with exception handling."""
        task: asyncio.Task = asyncio.get_event_loop().create_task(
            self._safe_background_task(coro, name)
        )

# СТАЛО:
    def _create_background_task(self, coro: Any, name: str = "") -> asyncio.Task:
        """Create a tracked background task with exception handling."""
        task: asyncio.Task = asyncio.get_running_loop().create_task(
            self._safe_background_task(coro, name)
        )
```

- [ ] **Step 5: Запустить тесты**

```bash
uv run pytest tests/unit/test_production_broadcast.py -v
uv run pytest -v -x
```
Ожидаем: PASS

- [ ] **Step 6: Коммит**

```bash
git add src/docuflow/features/production/system.py \
        src/docuflow/features/admin/system.py \
        tests/unit/test_production_broadcast.py
git commit -m "fix: wrap unawaited async broadcast in create_task, replace deprecated get_event_loop"
```

---

### Task 5: Исправить mirror loop — двойной fetch settings

**Баг:** `mirror.py:82` — второй fetch вне try/except убивает loop при ошибке.

**Files:**
- Modify: `src/docuflow/features/folder_scanner/mirror.py:70-83`
- Test: `tests/unit/test_mirror_loop.py`

- [ ] **Step 1: Написать тест**

```python
# tests/unit/test_mirror_loop.py
from unittest.mock import AsyncMock, MagicMock, patch, call
import pytest
import asyncio
from docuflow.features.folder_scanner.mirror import NSMirrorService


@pytest.fixture
def mirror_service(tmp_path):
    from docuflow.infrastructure.config import Config
    from sqlalchemy import create_engine
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    engine = MagicMock()
    sdk = AsyncMock()
    return NSMirrorService(config, sdk, engine)


@pytest.mark.asyncio
async def test_mirror_loop_survives_settings_fetch_error(mirror_service):
    """Loop должен продолжаться даже если первый fetch settings падает."""
    from docuflow.features.folder_scanner.settings import FolderScannerSettings

    call_count = 0

    async def fetch_side_effect(cls):
        nonlocal call_count
        call_count += 1
        if call_count == 1:
            raise ConnectionError("Network error")
        mirror_service._running = False  # остановить после 2-го вызова
        return FolderScannerSettings(ns_mirror_interval_seconds=0.01)

    mirror_service.sdk.resolve_system_by_type = fetch_side_effect
    mirror_service._running = True

    # Не должен падать с исключением
    await asyncio.wait_for(mirror_service._mirror_loop(), timeout=1.0)
    assert call_count >= 2, "Loop должен продолжаться после ошибки"


@pytest.mark.asyncio
async def test_mirror_loop_single_settings_fetch_per_cycle(mirror_service):
    """Settings должны загружаться только один раз за цикл."""
    from docuflow.features.folder_scanner.settings import FolderScannerSettings

    fetch_count = 0

    async def fetch_side_effect(cls):
        nonlocal fetch_count
        fetch_count += 1
        if fetch_count >= 2:
            mirror_service._running = False
        return FolderScannerSettings(ns_mirror_interval_seconds=0.01)

    mirror_service.sdk.resolve_system_by_type = fetch_side_effect
    mirror_service._running = True

    await asyncio.wait_for(mirror_service._mirror_loop(), timeout=1.0)
    # Один fetch на один цикл (не два)
    assert fetch_count == 2  # 1 fetch per iteration × 2 iterations
```

- [ ] **Step 2: Запустить — убедиться что первый тест падает**

```bash
uv run pytest tests/unit/test_mirror_loop.py -v
```
Ожидаем: FAIL — loop умирает при ошибке второго fetch

- [ ] **Step 3: Исправить `mirror.py` — убрать дублированный fetch**

```python
# src/docuflow/features/folder_scanner/mirror.py, строки ~70-83
# БЫЛО:
    async def _mirror_loop(self) -> None:
        """Periodic polling of the node's task bucket."""
        while self._running:
            try:
                settings: FolderScannerSettings = await self.sdk.resolve_system_by_type(
                    FolderScannerSettings
                )
                if settings.local_ns_path:
                    await self._sync_bucket(settings)
            except Exception as e:
                logger.error(f"Error in NS Mirror loop: {e}", exc_info=True)

            settings = await self.sdk.resolve_system_by_type(FolderScannerSettings)
            await asyncio.sleep(settings.ns_mirror_interval_seconds)

# СТАЛО:
    async def _mirror_loop(self) -> None:
        """Periodic polling of the node's task bucket."""
        default_interval: float = 30.0
        while self._running:
            interval: float = default_interval
            try:
                settings: FolderScannerSettings = await self.sdk.resolve_system_by_type(
                    FolderScannerSettings
                )
                interval = settings.ns_mirror_interval_seconds
                if settings.local_ns_path:
                    await self._sync_bucket(settings)
            except Exception as e:
                logger.error(f"Error in NS Mirror loop: {e}", exc_info=True)

            await asyncio.sleep(interval)
```

- [ ] **Step 4: Запустить тесты**

```bash
uv run pytest tests/unit/test_mirror_loop.py -v
```
Ожидаем: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/docuflow/features/folder_scanner/mirror.py \
        tests/unit/test_mirror_loop.py
git commit -m "fix: eliminate double settings fetch in mirror loop, protect interval from unhandled exception"
```

---

### Task 6: Исправить несоответствие имён ролей Admin/Админ

**Баги:**
- `auth/system.py:61` — создаёт английскую "Admin" роль вместо кириллической "Админ"
- `admin/system.py:489` — `AdminSyncSystem._is_admin` не защищает кириллический "Админ"

**Files:**
- Modify: `src/docuflow/features/auth/system.py:61-63`
- Modify: `src/docuflow/features/admin/system.py:489-490`
- Test: `tests/unit/test_auth_admin_roles.py`

- [ ] **Step 1: Написать failing тесты**

```python
# tests/unit/test_auth_admin_roles.py
import pytest
from sqlmodel import Session, create_engine, SQLModel
from docuflow.infrastructure.config import Config
from docuflow.features.auth.system import AuthSystem
from docuflow.features.admin.system import AdminSyncSystem


@pytest.fixture
def db_session():
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)
    with Session(engine) as session:
        yield session


@pytest.fixture
def auth_system(db_session, tmp_path):
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    return AuthSystem(config, db_session)


def test_get_or_create_admin_uses_cyrillic_role(auth_system):
    """get_or_create_admin должен искать/создавать кириллическую роль 'Админ'."""
    admin = auth_system.get_or_create_admin("test_pass")
    from docuflow.domain.entities.identity import Role
    role = auth_system.db_session.get(Role, admin.role_id)
    assert role is not None
    assert role.name == "Админ", f"Ожидали 'Админ', получили '{role.name}'"


def test_admin_sync_is_admin_rejects_cyrillic(auth_system):
    """AdminSyncSystem._is_admin должен защищать и 'Админ' и 'admin'."""
    sync = AdminSyncSystem(MagicMock())
    assert sync._is_admin("Админ") is True, "Кириллический Админ должен быть защищён"
    assert sync._is_admin("admin") is True
    assert sync._is_admin("Admin") is True
    assert sync._is_admin("ADMIN") is True
    assert sync._is_admin("operator") is False


def test_admin_sync_cannot_delete_cyrillic_admin_role(auth_system):
    """handle_delete_role не должен удалять кириллическую роль 'Админ'."""
    from unittest.mock import MagicMock
    engine = MagicMock()
    sync = AdminSyncSystem(engine)
    # Не должен даже открывать сессию для удаления
    sync.handle_delete_role({"name": "Админ"})
    engine.connect.assert_not_called()
```

- [ ] **Step 2: Запустить — убедиться что тесты падают**

```bash
uv run pytest tests/unit/test_auth_admin_roles.py -v
```
Ожидаем: FAIL — `get_or_create_admin` создаёт English "Admin", `_is_admin` не защищает "Админ"

- [ ] **Step 3: Исправить `auth/system.py:61-63`**

```python
# src/docuflow/features/auth/system.py, строки ~60-75
# БЫЛО:
    def get_or_create_admin(self, default_password: str | None = None) -> User:
        # 1. Ensure Admin role exists
        admin_role: Role | None = self.find_one(Role, name="Admin")
        if not admin_role:
            admin_role = self.save(Role(name="Admin", permissions=json.dumps(["*:full"])))

# СТАЛО:
    def get_or_create_admin(self, default_password: str | None = None) -> User:
        # 1. Ensure Admin role exists — use Cyrillic name consistent with seed_default_roles
        admin_role: Role | None = self.find_one(Role, name="Админ")
        if not admin_role:
            # Fallback: check English name for backwards compatibility
            admin_role = self.find_one(Role, name="Admin")
        if not admin_role:
            admin_role = self.save(Role(name="Админ", permissions=json.dumps(["*:full"])))
```

- [ ] **Step 4: Исправить `admin/system.py:489-490` — `AdminSyncSystem._is_admin`**

```python
# src/docuflow/features/admin/system.py, строки ~489-490
# БЫЛО:
    def _is_admin(self, name: str) -> bool:
        return name.strip().lower() == "admin"

# СТАЛО:
    def _is_admin(self, name: str) -> bool:
        n: str = name.strip().lower()
        return n in ("admin", "админ")
```

- [ ] **Step 5: Запустить тесты**

```bash
uv run pytest tests/unit/test_auth_admin_roles.py -v
```
Ожидаем: PASS

- [ ] **Step 6: Коммит**

```bash
git add src/docuflow/features/auth/system.py \
        src/docuflow/features/admin/system.py \
        tests/unit/test_auth_admin_roles.py
git commit -m "fix: align admin role name to Cyrillic 'Админ', protect from P2P deletion"
```

---

### Task 7: Исправить FK violation при reorder (inventory)

**Баг:** `inventory/system.py:315` — `MaterialAudit(stock_item_id=0)` нарушает FK constraint.

**Files:**
- Modify: `src/docuflow/features/inventory/system.py:313-320`
- Test: `tests/unit/test_inventory_reorder.py`

- [ ] **Step 1: Написать failing тест**

```python
# tests/unit/test_inventory_reorder.py
import pytest
from sqlmodel import Session, create_engine, SQLModel
from docuflow.infrastructure.config import Config
from docuflow.features.inventory.system import InventorySystem
from docuflow.domain.entities.production import MaterialType


@pytest.fixture
def inventory_system(tmp_path):
    from sqlalchemy import event as sa_event
    engine = create_engine(
        "sqlite:///:memory:",
        connect_args={"check_same_thread": False}
    )

    @sa_event.listens_for(engine, "connect")
    def set_pragma(conn, _):
        conn.execute("PRAGMA foreign_keys=ON")

    SQLModel.metadata.create_all(engine)
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    session = Session(engine)
    return InventorySystem(config, session)


def test_request_material_reorder_no_fk_violation(inventory_system):
    """request_material_reorder не должен создавать MaterialAudit с stock_item_id=0."""
    # Arrange: создаём материал
    mat = MaterialType(code="ALU-3", thickness=3.0)
    inventory_system.db_session.add(mat)
    inventory_system.db_session.flush()
    inventory_system.db_session.refresh(mat)

    # Act: не должно бросить IntegrityError
    inventory_system.request_material_reorder(
        mat_type_id=mat.id,
        quantity=100,
        note="Low stock",
        author="test_user"
    )
    inventory_system.db_session.commit()

    # Assert: ChatMessage создан, аудит создан без FK нарушения
    from docuflow.domain.entities.production import MaterialAudit, ChatMessage
    from sqlmodel import select
    audits = list(inventory_system.db_session.exec(select(MaterialAudit)).all())
    assert len(audits) == 1
    # stock_item_id не должен быть 0
    assert audits[0].stock_item_id != 0 or audits[0].stock_item_id is None
```

- [ ] **Step 2: Запустить — убедиться что тест падает**

```bash
uv run pytest tests/unit/test_inventory_reorder.py -v
```
Ожидаем: FAIL — `IntegrityError: FOREIGN KEY constraint failed`

- [ ] **Step 3: Исправить `inventory/system.py:313-320`**

```python
# src/docuflow/features/inventory/system.py, строки ~312-323
# БЫЛО:
        # 1. Permanent Audit
        audit_entry: MaterialAudit = MaterialAudit(
            stock_item_id=0,
            operation="reorder",
            qty_delta=quantity,
            reason=note,
            author=author,
            node_id=self.config.node_id,
        )
        session.add(audit_entry)

# СТАЛО:
        # 1. Permanent Audit (WorkLog вместо MaterialAudit — нет stock_item для reorder)
        from docuflow.domain.entities.production import WorkLog, WorkLogType
        audit_log: WorkLog = WorkLog(
            log_type=WorkLogType.INFO,
            message=f"[REORDER_REQUEST] {mat_label} qty={quantity} note={note}",
            author=author,
            node_id=self.config.node_id,
        )
        session.add(audit_log)
```

- [ ] **Step 4: Запустить тесты**

```bash
uv run pytest tests/unit/test_inventory_reorder.py -v
```
Ожидаем: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/docuflow/features/inventory/system.py \
        tests/unit/test_inventory_reorder.py
git commit -m "fix: replace MaterialAudit(stock_item_id=0) with WorkLog for reorder requests"
```

---

### Task 8: Исправить _write_snapshot_atomically (sync)

**Баг:** `sync.py:141` — запись не атомарная, при сбое снэпшот повреждён.

**Files:**
- Modify: `src/docuflow/infrastructure/sync.py:141-150`
- Test: `tests/unit/test_sync_atomic_write.py`

- [ ] **Step 1: Написать тест**

```python
# tests/unit/test_sync_atomic_write.py
import json
from pathlib import Path
import pytest
from unittest.mock import patch, AsyncMock


@pytest.mark.asyncio
async def test_write_snapshot_atomically_uses_temp_then_rename(tmp_path):
    """Запись снэпшота должна использовать temp файл и os.replace."""
    from docuflow.infrastructure.config import Config
    from docuflow.infrastructure.sync import DataSyncSystem
    from unittest.mock import MagicMock
    import os

    config = Config(node_id="TEST", shared_path=str(tmp_path))
    system = DataSyncSystem(config, MagicMock())

    target = tmp_path / "test_snapshot.json"
    data = {"users": [{"id": 1, "name": "Test"}]}

    await system._write_snapshot_atomically(target, data)

    # Файл должен существовать
    assert target.exists()

    # Содержимое должно быть корректным JSON
    content = json.loads(target.read_text())
    assert content == data

    # Temp файл должен быть удалён
    temp = tmp_path / "TEMP_test_snapshot.json"
    assert not temp.exists(), "Временный файл должен быть удалён после rename"


@pytest.mark.asyncio
async def test_write_snapshot_atomically_no_corrupt_on_failure(tmp_path):
    """При сбое записи существующий снэпшот не должен быть повреждён."""
    from docuflow.infrastructure.config import Config
    from docuflow.infrastructure.sync import DataSyncSystem
    from unittest.mock import MagicMock
    import json

    config = Config(node_id="TEST", shared_path=str(tmp_path))
    system = DataSyncSystem(config, MagicMock())

    # Существующий снэпшот
    target = tmp_path / "existing.json"
    original_data = {"users": [{"id": 1}]}
    target.write_text(json.dumps(original_data))

    # Симулируем сбой при записи
    with patch("anyio.Path.write_text", side_effect=OSError("disk full")):
        with pytest.raises(OSError):
            await system._write_snapshot_atomically(target, {"new": "data"})

    # Оригинальный файл должен быть нетронут
    assert json.loads(target.read_text()) == original_data
```

- [ ] **Step 2: Запустить — убедиться что второй тест падает (нет защиты)**

```bash
uv run pytest tests/unit/test_sync_atomic_write.py -v
```
Ожидаем: первый тест FAIL (нет temp rename), второй — может FAIL

- [ ] **Step 3: Исправить `sync.py:141-150`**

```python
# src/docuflow/infrastructure/sync.py, строки ~141-150
# БЫЛО:
    async def _write_snapshot_atomically(self, path: Path, data: dict) -> None:
        """Serialize data to JSON and write to the shared drive."""
        def _json_serial(obj: Any) -> Any:
            if isinstance(obj, datetime.datetime):
                return obj.isoformat()
            raise TypeError(f"Type {type(obj)} is not serializable")

        serialized_json: str = json.dumps(data, default=_json_serial, indent=2)
        await anyio.Path(path).write_text(serialized_json)

# СТАЛО:
    async def _write_snapshot_atomically(self, path: Path, data: dict) -> None:
        """Serialize data to JSON using temp+rename for crash safety."""
        import os

        def _json_serial(obj: Any) -> Any:
            if isinstance(obj, datetime.datetime):
                return obj.isoformat()
            raise TypeError(f"Type {type(obj)} is not serializable")

        serialized_json: str = json.dumps(data, default=_json_serial, indent=2)
        temp_path: Path = path.with_name(f"TEMP_{path.name}")
        try:
            await anyio.Path(temp_path).write_text(serialized_json)
            os.replace(temp_path, path)
        except Exception:
            if temp_path.exists():
                temp_path.unlink(missing_ok=True)
            raise
```

- [ ] **Step 4: Запустить тесты**

```bash
uv run pytest tests/unit/test_sync_atomic_write.py -v
```
Ожидаем: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/docuflow/infrastructure/sync.py \
        tests/unit/test_sync_atomic_write.py
git commit -m "fix: make _write_snapshot_atomically actually atomic via temp+rename"
```

---

### Task 9: Исправить вложенные сессии в get_node_drift

**Баг:** `task_board/system.py:472` — `get_node_drift` открывает сессию и вызывает `get_bucket`, который открывает ещё одну.

**Files:**
- Modify: `src/docuflow/features/task_board/system.py:470-484`
- Test: `tests/unit/test_task_board_drift.py`

- [ ] **Step 1: Написать тест**

```python
# tests/unit/test_task_board_drift.py
import pytest
from sqlmodel import Session, create_engine, SQLModel


@pytest.fixture
def task_board(tmp_path):
    from docuflow.infrastructure.config import Config
    from docuflow.features.task_board.system import TaskBoardSystem
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    session = Session(engine)
    return TaskBoardSystem(config, engine, session)


def test_get_node_drift_empty_bucket(task_board):
    """get_node_drift возвращает 0.0 для пустого bucket."""
    result = task_board.get_node_drift("NODE_1")
    assert result == 0.0


def test_get_node_drift_with_completed_tasks(task_board, tmp_path):
    """get_node_drift корректно считает дрейф без nested session errors."""
    import datetime
    from docuflow.domain.entities.production import (
        Project, WorkItem, TaskItem, TaskItemStatus, WorkerBucketEntry
    )
    from sqlmodel import Session, create_engine, SQLModel
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)

    with Session(engine) as session:
        project = Project(name="Test")
        session.add(project)
        session.flush()
        wi = WorkItem(folder_name="test", folder_path=".", project_id=project.id)
        session.add(wi)
        session.flush()
        task = TaskItem(
            work_item_id=wi.id,
            file_name="t.gnc",
            file_path="t.gnc",
            status=TaskItemStatus.DONE,
            estimated_minutes=60,
            actual_minutes=90,
            assigned_to_node="NODE_1",
        )
        session.add(task)
        session.flush()
        bucket = WorkerBucketEntry(
            node_id="NODE_1",
            task_item_id=task.id,
        )
        session.add(bucket)
        session.commit()

    from docuflow.infrastructure.config import Config
    from docuflow.features.task_board.system import TaskBoardSystem
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    board = TaskBoardSystem(config, engine)

    drift = board.get_node_drift("NODE_1")
    assert drift == pytest.approx(50.0)  # (90-60)/60 * 100 = 50%
```

- [ ] **Step 2: Запустить — убедиться что второй тест работает (или выявить проблему)**

```bash
uv run pytest tests/unit/test_task_board_drift.py -v
```

- [ ] **Step 3: Исправить `task_board/system.py:470-484`**

```python
# src/docuflow/features/task_board/system.py, строки ~470-484
# БЫЛО (nested sessions):
    def get_node_drift(self, node_id: str) -> float:
        with self.get_db_session() as session:
            bucket_entries = self.get_bucket(node_id)  # ← открывает вторую сессию!
            total_estimated = 0
            total_actual = 0
            for entry in bucket_entries:
                task = session.get(TaskItem, entry.task_item_id)
                ...

# СТАЛО (одна сессия):
    def get_node_drift(self, node_id: str) -> float:
        """Calculates the average Drift % for all completed tasks on a node."""
        with self.get_db_session() as session:
            bucket_entries = list(
                session.exec(
                    select(WorkerBucketEntry).where(WorkerBucketEntry.node_id == node_id)
                ).all()
            )
            total_estimated = 0
            total_actual = 0
            for entry in bucket_entries:
                task = session.get(TaskItem, entry.task_item_id)
                if task and task.status == TaskItemStatus.DONE:
                    total_estimated += task.estimated_minutes or 0
                    total_actual += task.actual_minutes or 0

            if total_estimated == 0:
                return 0.0
            return (total_actual - total_estimated) / total_estimated * 100
```

- [ ] **Step 4: Запустить тесты**

```bash
uv run pytest tests/unit/test_task_board_drift.py -v
```
Ожидаем: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/docuflow/features/task_board/system.py \
        tests/unit/test_task_board_drift.py
git commit -m "fix: eliminate nested sessions in get_node_drift"
```

---

### Task 10: Исправить incident system — неверные запросы

**Баги:**
- `incidents.py:207` — `get_active_failures` сортирует по `resolved_at` (NULL)
- `incidents.py:188` — `get_summary_stats` использует `.isnot(False)` вместо `.is_(True)`

**Files:**
- Modify: `src/docuflow/features/chat/incidents.py:188` и `incidents.py:207`
- Test: `tests/unit/test_incident_queries.py`

- [ ] **Step 1: Написать failing тесты**

```python
# tests/unit/test_incident_queries.py
import datetime
import pytest
from sqlmodel import Session, create_engine, SQLModel
from docuflow.infrastructure.config import Config
from docuflow.features.chat.incidents import IncidentSystem
from docuflow.domain.entities.production import IncidentLog


@pytest.fixture
def incident_system(tmp_path):
    from unittest.mock import MagicMock
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    session = Session(engine)
    return IncidentSystem(config, session, chat_system=MagicMock())


def _add_incident(session, resolved: bool | None, incident_type: str = "BREAKDOWN") -> IncidentLog:
    inc = IncidentLog(
        incident_type=incident_type,
        description="test",
        reported_by="test",
        node_id="TEST",
        resolved=resolved,
    )
    session.add(inc)
    session.flush()
    return inc


def test_get_active_failures_excludes_resolved(incident_system):
    """get_active_failures не должен возвращать разрешённые инциденты."""
    session = incident_system.db_session
    _add_incident(session, resolved=False)
    _add_incident(session, resolved=True)
    _add_incident(session, resolved=None)
    session.commit()

    active = incident_system.get_active_failures()
    assert len(active) == 1
    assert active[0].resolved is False


def test_get_summary_stats_excludes_null_resolved(incident_system):
    """get_summary_stats должен считать только resolved=True инциденты."""
    session = incident_system.db_session
    inc_resolved = _add_incident(session, resolved=True)
    inc_resolved.downtime_minutes = 30.0
    _add_incident(session, resolved=False)  # не должен попасть
    _add_incident(session, resolved=None)   # не должен попасть
    session.commit()

    stats = incident_system.get_summary_stats()
    total = sum(stats.values())
    assert total == 30.0, f"Ожидали 30.0 (только resolved=True), получили {total}"
```

- [ ] **Step 2: Запустить — убедиться что тесты падают**

```bash
uv run pytest tests/unit/test_incident_queries.py -v
```
Ожидаем: FAIL — в активных есть resolved=None, в stats включены NULL-инциденты

- [ ] **Step 3: Исправить `incidents.py`**

```python
# src/docuflow/features/chat/incidents.py, строка ~187-210

# --- get_summary_stats: БЫЛО .isnot(False) → СТАЛО .is_(True) ---
    def get_summary_stats(self) -> dict[str, float]:
        session: Session = self.db_session
        statement: SelectOfScalar[IncidentLog] = select(IncidentLog).where(
            IncidentLog.resolved.is_(True)  # type: ignore[union-attr]
        )
        resolved_incidents: Sequence[IncidentLog] = session.exec(statement).all()

        stats: dict[str, float] = {}
        for incident in resolved_incidents:
            stats[incident.incident_type] = stats.get(incident.incident_type, 0.0) + (
                incident.downtime_minutes or 0.0
            )
        return stats

# --- get_active_failures: БЫЛО сортировка по resolved_at → СТАЛО created_at ---
    def get_active_failures(self, limit: int = 20) -> list[IncidentLog]:
        session: Session = self.db_session
        statement: SelectOfScalar[IncidentLog] = (
            select(IncidentLog)
            .where(IncidentLog.resolved.is_(False))  # type: ignore[union-attr]
            .order_by(IncidentLog.created_at.desc())  # type: ignore[union-attr]
            .limit(limit)
        )
        return list(session.exec(statement).all())
```

- [ ] **Step 4: Запустить тесты**

```bash
uv run pytest tests/unit/test_incident_queries.py -v
```
Ожидаем: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/docuflow/features/chat/incidents.py \
        tests/unit/test_incident_queries.py
git commit -m "fix: use is_(True)/is_(False) for resolved bool, sort active failures by created_at"
```

---

### Task 11: Исправить datetime detection в sync и node_id matching в bus

**Баги:**
- `sync.py:179` — `"T" in value` слишком широко
- `bus.py:279` — хрупкий substring match для node_id

**Files:**
- Modify: `src/docuflow/infrastructure/sync.py:176-183`
- Modify: `src/docuflow/infrastructure/bus.py:276-288`
- Test: `tests/unit/test_sync_datetime.py`, `tests/unit/test_bus_routing.py`

- [ ] **Step 1: Написать тесты**

```python
# tests/unit/test_sync_datetime.py
from docuflow.infrastructure.sync import DataSyncSystem
from unittest.mock import MagicMock
import datetime


def make_sync(tmp_path):
    from docuflow.infrastructure.config import Config
    config = Config(node_id="TEST", shared_path=str(tmp_path))
    return DataSyncSystem(config, MagicMock())


def test_station_name_not_parsed_as_datetime(tmp_path):
    """Строки типа 'STATION_A' не должны пытаться парситься как datetime."""
    sync = make_sync(tmp_path)
    row_data = {
        "node_id": "STATION_A",
        "module": "STATUS_CHANGE",
        "key": "active",
        "value": "true",
    }
    # _process_remote_row не должен испортить строки с "T"
    from docuflow.domain.entities.identity import NodeSetting
    from sqlmodel import Session, create_engine, SQLModel
    engine = create_engine("sqlite:///:memory:")
    SQLModel.metadata.create_all(engine)
    with Session(engine) as session:
        sync._process_remote_row(session, NodeSetting, row_data, ["id"])
        # node_id должен остаться строкой
        result = session.exec(
            __import__('sqlmodel').select(NodeSetting)
        ).first()
        if result:
            assert result.node_id == "STATION_A"
            assert result.module == "STATUS_CHANGE"


# tests/unit/test_bus_routing.py
from docuflow.infrastructure.bus import FileBusSystem
from docuflow.infrastructure.config import Config
from docuflow.infrastructure import constants


def make_bus(node_id: str, tmp_path) -> FileBusSystem:
    config = Config(node_id=node_id, shared_path=str(tmp_path))
    return FileBusSystem(config)


def test_node_id_not_false_positive_substring(tmp_path):
    """node_id 'A' не должен ложно совпадать с именем файла для 'STATION_A'."""
    bus = make_bus("A", tmp_path)
    # Файл для STATION_A, не для A
    filename = f"REQ_SENDER{constants.BUS_DELIMITER}STATION_A{constants.BUS_DELIMITER}001.json"
    result = bus._is_relevant_message(filename, constants.BUS_INBOX_DIR)
    assert result is False, "node_id 'A' не должен матчить 'STATION_A'"


def test_node_id_exact_match(tmp_path):
    """Точное совпадение node_id должно работать."""
    bus = make_bus("STATION_A", tmp_path)
    filename = (
        f"REQ_SENDER{constants.BUS_DELIMITER}"
        f"STATION_A{constants.BUS_DELIMITER}001.json"
    )
    result = bus._is_relevant_message(filename, constants.BUS_INBOX_DIR)
    assert result is True
```

- [ ] **Step 2: Запустить — убедиться что тест bus падает**

```bash
uv run pytest tests/unit/test_sync_datetime.py tests/unit/test_bus_routing.py -v
```
Ожидаем: `test_node_id_not_false_positive_substring` FAIL

- [ ] **Step 3: Исправить `sync.py:176-183`**

```python
# src/docuflow/infrastructure/sync.py, строки ~175-183
# БЫЛО:
        for key, value in row_copy.items():
            if isinstance(value, str) and "T" in value:
                try:
                    row_copy[key] = datetime.datetime.fromisoformat(value)
                except ValueError:
                    pass

# СТАЛО: проверяем ISO 8601 формат явно (минимум 10 символов, содержит '-')
        for key, value in row_copy.items():
            if (
                isinstance(value, str)
                and len(value) >= 10
                and value[4:5] == "-"
                and value[7:8] == "-"
            ):
                try:
                    row_copy[key] = datetime.datetime.fromisoformat(value)
                except ValueError:
                    pass
```

- [ ] **Step 4: Исправить `bus.py:276-288`**

```python
# src/docuflow/infrastructure/bus.py, строки ~276-288
# БЫЛО:
        is_addressed_to_me: bool = (
            f"{constants.BUS_DELIMITER}{self._node_id}{constants.BUS_DELIMITER}" in filename
        )

        prefix: str = (
            constants.BUS_PREFIX_REQ
            if folder_name == constants.BUS_INBOX_DIR
            else constants.BUS_PREFIX_RES
        )
        return is_addressed_to_me and filename.startswith(prefix)

# СТАЛО: разбираем имя файла по разделителям
        prefix: str = (
            constants.BUS_PREFIX_REQ
            if folder_name == constants.BUS_INBOX_DIR
            else constants.BUS_PREFIX_RES
        )
        if not filename.startswith(prefix):
            return False

        # Структура: {PREFIX}{FROM}{DELIM}{TO}{DELIM}{ID}.json
        # Убираем префикс и расширение
        stem: str = filename[len(prefix):-len(constants.BUS_EXTENSION)]
        parts: list[str] = stem.split(constants.BUS_DELIMITER)
        # parts = [FROM, TO, ID]
        if len(parts) < 3:
            return False
        return parts[1] == self._node_id
```

- [ ] **Step 5: Запустить все тесты**

```bash
uv run pytest tests/unit/test_sync_datetime.py tests/unit/test_bus_routing.py -v
```
Ожидаем: PASS

- [ ] **Step 6: Финальный прогон всего test suite**

```bash
uv run pytest -v --tb=short
```
Ожидаем: все тесты PASS, нет регрессий

- [ ] **Step 7: Коммит**

```bash
git add src/docuflow/infrastructure/sync.py \
        src/docuflow/infrastructure/bus.py \
        tests/unit/test_sync_datetime.py \
        tests/unit/test_bus_routing.py
git commit -m "fix: use ISO format check for datetime detection, fix node_id exact match in bus routing"
```

---

## Итог

| Таск | Баги | Файлы затронуты |
|---|---|---|
| T1 | broadcast_request, broadcast_message, unused import | sync.py, chat/system.py, admin/system.py |
| T2 | Failed messages не удаляются | orchestrator.py |
| T3 | Sequence race + двойная регистрация | dispatcher.py, orchestrator.py |
| T4 | Missing await, get_event_loop deprecated | production/system.py, admin/system.py |
| T5 | Mirror loop двойной fetch | mirror.py |
| T6 | Admin role name mismatch | auth/system.py, admin/system.py |
| T7 | FK violation при reorder | inventory/system.py |
| T8 | Неатомарная запись снэпшота | sync.py |
| T9 | Nested sessions в get_node_drift | task_board/system.py |
| T10 | Incident query bugs | chat/incidents.py |
| T11 | datetime detection + bus routing | sync.py, bus.py |
